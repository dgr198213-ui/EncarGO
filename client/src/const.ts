export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// NOTE: la autenticación real (Supabase Auth) se implementa en la fase de
// backend del MVP — ver §8 de la arquitectura. El flujo OAuth de Manus que
// vivía aquí se ha retirado por no ser el proveedor de auth del producto.
